import numpy as np
from layer import layers
class backpropogation:
   # looks at one layer
    def backpropogation(layer):
       x = 0
       #place holder for true value of prediction
       y = 0
       # finds the derivitve between layers. aka it find the indiviual links in the chain rule
       for i in range(len(layers.object_list), 0, -1):
            #if(i == np.max(layers.object_list)):
              # x = (y - layers.activation_list[i]) * 
            


                    
               
           
                                                          

           


    

    